/*
Author: Fernando Zuher
Place: Brazil
Date: 25/03/2020
Book: The C programming language, second edition. Authors: BRIAN W KERNIGHAN and DENNIS M. RITCHIE
About: Exercise, Chapter 5 - Pointers and Arrays
*/

/*
Exercise 5-1. As written, getint treats a + or - not followed by a digit as a valid
representation of zero. Fix it to push such a character back on the input.
*/
	***I didn't understand the question.
		if (!isdigit(c) && c != EOF && c != '+' && c != '-')

	"+ or - not followed by a digit as a valid representation of zero"
		Actually it doesn't represent, do it?

