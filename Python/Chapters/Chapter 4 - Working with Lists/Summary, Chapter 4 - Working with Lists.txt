/*
Author: Fernando Zuher
Place: Brazil
Date: 26/02/2020
Book: Python Crash Course, 2nd Edition. Author: ERIC MATTHES.
About: Summary, Chapter 4 - Working with Lists
*/

for value in values:
	...

for value in range(1, 6):

for value in range(6):

variable = list(range(1, 6))
list(range(2, 11, 2))

number ** 2

min(lista)
max(lista)
sum(lista)

squares = [value ** 2 for value in range(1,11)]

players = [1, 2, 3, 4, 5]
print(players[0:2])
print(players[1:3])
print(players[1:])
print(players[:3])
print(players[-3:])

for player in players[:3]:

#faz uma cópia
lista1 = lista2[:]

tuple

tupla = (200, 50)
#Com apenas um elemento:
tupla = (200,)

Diretrizes para escrita de códigos em Python:
Indentação com 4 espaços. Antes de fechar o código escrito de uma vez, a gente pode substituir os tabs por 4 espaços de uma vez só usando o Sublime.

Ver Apêndice B.

Comprimento da linha de no máximo 79/99 caracteres.
Comentários de até 72 caracteres.

Linhas em branco para facilitar leitura.

